# Supported HPC platforms for templater.
# Adjust by adding an option here, then creating a corresponding
#  directory in ./platforms
platforms = ["bebop", "summit", "theta", "cori"]

# Supported HPC tests.
# Adjust by adding an option here, then creating corresponding
#  directories inside platform directories.
tests = ["forces", "warpx"]
